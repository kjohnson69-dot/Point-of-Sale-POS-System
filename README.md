# Point-of-Sale-POS-System
A Point of Sale (POS) System that cashiers can add items to a shopping cart, remove items, view the cart, calculate the total bill (including  tax),  accept payment, and generate a receipt.
